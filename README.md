# Realgazeta SDK

Realgazeta API client, generated from the OpenAPI spec.

The Realgazeta API provides access to thoughts, stories, and ideas from the realgazeta.com.ua website.

Learn more about Realgazeta API at [github.com/l0v3m0n3y/realgazeta](https://github.com/l0v3m0n3y/realgazeta).

This is an unofficial SDK for the Realgazeta public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Metadata kindly supplied by [www.freepublicapis.com](https://www.freepublicapis.com).

> TypeScript, Python, PHP, Golang, Ruby, Lua SDKs, a CLI, an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — Author — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`load`):

```ts
const client = new RealgazetaSDK()
const author = await client.Author().load()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
const client = RealgazetaSDK.test()
const author = await client.Author().load({ slug: 'example_slug' })
// author is a bare Author populated with mock data
console.log(author)
```

### Python

```python
client = RealgazetaSDK.test()
author = client.Author().load({"slug": "example"})
print(author)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = RealgazetaSDK::test([
    "entity" => ["author" => ["test01" => []]],
]);
$author = $client->Author()->load(["slug" => "example"]);
```

### Golang

```go
client := sdk.Test()
result, err := client.Author(nil).Load(
    nil, nil,
)
```

### Ruby

```ruby
# Seed fixture data so offline calls resolve without a live server.
client = RealgazetaSDK.test({
  "entity" => { "author" => { "test01" => {} } },
})
author = client.Author.load({ "slug" => "example" })
```

### Lua

```lua
local client = sdk.test()
local result, err = client:Author():load({ slug = "example" })
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/realgazeta` | publish pending — [install from git tag](https://github.com/voxgig-sdk/realgazeta-sdk/releases) |
| Python | `voxgig-sdk-realgazeta` | publish pending — [install from git tag](https://github.com/voxgig-sdk/realgazeta-sdk/releases) |
| PHP | `voxgig-sdk/realgazeta` | publish pending — [install from git tag](https://github.com/voxgig-sdk/realgazeta-sdk/releases) |
| Golang | `github.com/voxgig-sdk/realgazeta-sdk/go` | `go get github.com/voxgig-sdk/realgazeta-sdk/go@latest` |
| Ruby | `voxgig-sdk-realgazeta` | publish pending — [install from git tag](https://github.com/voxgig-sdk/realgazeta-sdk/releases) |
| Lua | `voxgig-sdk-realgazeta` | publish pending — [install from git tag](https://github.com/voxgig-sdk/realgazeta-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/realgazeta-sdk/go-cli` | `go install github.com/voxgig-sdk/realgazeta-sdk/go-cli/cmd/realgazeta@latest` |
| Go MCP server | `github.com/voxgig-sdk/realgazeta-sdk/go-mcp` | `go get github.com/voxgig-sdk/realgazeta-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { RealgazetaSDK } from '@voxgig-sdk/realgazeta'

const client = new RealgazetaSDK({
  apikey: process.env.REALGAZETA_APIKEY,
})


// Load a specific author (returns a Author)
const author = await client.Author().load({
  slug: 'example_slug',
})
console.log(author)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Ruby, Lua) | `ts/` `py/` `php/` `go/` `rb/` `lua/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o realgazeta-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "realgazeta": {
      "command": "/abs/path/to/realgazeta-mcp"
    }
  }
}
```

## Entities

The API exposes one entity:

| Entity | Description | API path |
| --- | --- | --- |
| **Author** | The Author entity (load). | `/authors/slug/{slug}/` |

The operations available across these entities are **load** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from realgazeta_sdk import RealgazetaSDK

client = RealgazetaSDK({
    "apikey": os.environ.get("REALGAZETA_APIKEY"),
})


# Load a specific author (returns the record, raises on error)
author = client.Author().load({"slug": "example_slug"})
print(author)
```

### PHP

```php
<?php
require_once 'realgazeta_sdk.php';

$client = new RealgazetaSDK([
    "apikey" => getenv("REALGAZETA_APIKEY"),
]);


// Load a specific author (returns the bare record; throws on error)
$author = $client->Author()->load(["slug" => "example_slug"]);
print_r($author);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/realgazeta-sdk/go"

client := sdk.NewRealgazetaSDK(map[string]any{
    "apikey": os.Getenv("REALGAZETA_APIKEY"),
})


// Load a specific author
author, err := client.Author(nil).Load(
    map[string]any{"slug": "example_slug"}, nil,
)
if err != nil {
    panic(err)
}
fmt.Println(author)
```

### Ruby

```ruby
require_relative "Realgazeta_sdk"

client = RealgazetaSDK.new({
  "apikey" => ENV["REALGAZETA_APIKEY"],
})


# Load a specific author (returns the bare record; raises on error)
author = client.Author.load({ "slug" => "example_slug" })
puts author
```

### Lua

```lua
local sdk = require("realgazeta_sdk")

local client = sdk.new({
  apikey = os.getenv("REALGAZETA_APIKEY"),
})


-- Load a specific author
local author, err = client:Author():load({ slug = "example_slug" })
print(author)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Ruby:**
```ruby
result = client.direct({
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => { "id" => "example" },
})
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Ruby](rb/README.md)
- [Lua](lua/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

- Upstream API: [https://github.com/l0v3m0n3y/realgazeta](https://github.com/l0v3m0n3y/realgazeta)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Realgazeta API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
